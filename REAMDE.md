```bash
git clone git@github.com:Nooumakk/django_tree_menu.git
cd django-tree-menu
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python manage.py makemigrations
python manage.py migrate
python manage.py runserver
```
